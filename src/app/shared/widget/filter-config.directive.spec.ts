import { FilterConfigDirective } from './filter-config.directive';

describe('FilterConfigDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterConfigDirective();
    expect(directive).toBeTruthy();
  });
});
